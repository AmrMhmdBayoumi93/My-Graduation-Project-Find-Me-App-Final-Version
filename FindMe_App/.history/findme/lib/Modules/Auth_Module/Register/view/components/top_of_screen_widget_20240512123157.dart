import 'package:flutter/material.dart';

class TopOfScreenWidget extends StatelessWidget {
  const TopOfScreenWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}